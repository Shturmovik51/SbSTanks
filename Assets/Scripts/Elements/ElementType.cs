using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace SbSTanks
{
    public enum ElementType
    {
        FireElement = 0,
        WaterElement = 1,
        GroundElement = 2
    }
}
